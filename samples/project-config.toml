# Example .nn/config.toml (project-level overrides)
# Place this in your project's .nn/ directory.

# Use a different schema for this project
schema = "ado"

[defaults]
sort_by = "priority"
group_by = "status"

# Override colors for this project only
# [status.colors]
# active = "32;1"
